in folder run:

//initial input
> lmp -in input.lammps

//improved input
> lmp -in input.min.lammps

// reading output of prev
> lmp -in input.md.lammps

// excersize
> lmp -in lost_atoms.lammps

VMD:
> vmd dump.lammpstrj